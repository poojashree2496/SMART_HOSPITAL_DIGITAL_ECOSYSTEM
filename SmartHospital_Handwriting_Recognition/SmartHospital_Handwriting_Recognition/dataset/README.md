# Dataset

Put handwritten line images inside `images/`.

Then replace `labels.csv` with:

image,text
image001.png,exact transcription of the image
image002.png,another transcription

Use UTF-8 encoding. Keep the transcription exactly aligned with the image.
